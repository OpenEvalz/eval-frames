[project]
name = "openevalz-eval-frames"
version = "0.1.0"
description = "OpenEvalz packaging of FRAMES: Factual Retrieval and Multi-hop Evaluation Set"
license = { text = "BUSL-1.1" }
requires-python = ">=3.10"
dependencies = [
  "inspect-ai==0.3.260",
  "trusted-router>=0.7.0",
  "pyyaml>=6",
]

[tool.openevalz]
eval = "frames"
execution_class = "plain"
cost_class = "low"
